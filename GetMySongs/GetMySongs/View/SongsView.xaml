<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GetMySongs.View.FavoritesView" Title="{Binding userName}">
    <ContentPage.Content>
        <StackLayout>
            <ActivityIndicator x:Name="ActivityIndicator" Color="Green" IsRunning="{Binding IsBusy, Mode=TwoWay}" IsVisible="{Binding IsBusy, Mode=TwoWay}"/>
            <CollectionView ItemsSource="{Binding userList}" >
                <!--GroupDisplayBinding="{Binding PerformareGroup}" IsGroupingEnabled="True"-->
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}" Detail="{Binding Performare}">
                            <TextCell.ContextActions>
                                <MenuItem Clicked="Download_Clicked"
                      CommandParameter="{Binding .}"
                      Text="Download" />
                            </TextCell.ContextActions>
                        </TextCell>
                       
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>